node {
    def app

    stage('Clone repository') {
        /* Let's make sure we have the repository cloned to our workspace */

        checkout scm
    }
    
  
  
   /* stage('SonarQube analys'is) {
    def scannerHome = tool 'SonarScanner 4.2.0.1873';
    withSonarQubeEnv('My SonarQube Server') { // If you have configured more than one global server connection, you can specify its name
      sh "${scannerHome}/bin/sonar-scanner"
    }
  }*/
  /*stage('SonarQube analys') {
          // requires SonarQube Scanner 4.2.0.1873+
          scannerHome = tool 'SonarQube Scanner 4.2.0.1873'
        }
        withSonarQubeEnv('SonarQube Scanner') {
          sh "${scannerHome}/bin/sonar-scanner"
        }*/
        
stage('Code scan') {
     sh '/apps/sonar-scanner-3.3.0.1492-linux/bin/sonar-scanner'
    }

    
    

     /*stage('NPM INSTALL'){
        sh 'npm install'
    }*/
     
    }